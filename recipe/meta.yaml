{% set name = "python-gnupg" %}
{% set version = "0.5.0" %}

package:
  name: {{ name }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 70758e387fc0e0c4badbcb394f61acbe68b34970a8fed7e0f7c89469fe17912a

build:
  # gnupg isn't available on windows
  skip: True  # [win]
  number: 0
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - pip
    - setuptools
    - wheel
    - python
  run:
    - python
    - gnupg

test:
  source_files:
    - test_gnupg.py
  requires:
   - pip
   - pytest
  imports:
    - gnupg
  commands:
    - pip check
    # Skip test_search_keys which the upstream maintainers indicate is unstable
    - export NO_EXTERNAL_TESTS=1
    - pytest -v

about:
  home: http://pythonhosted.org/python-gnupg
  license: BSD-3-Clause
  license_file: LICENSE.txt
  summary: A wrapper for the Gnu Privacy Guard (GPG or GnuPG)

extra:
  recipe-maintainers:
    - vamega
